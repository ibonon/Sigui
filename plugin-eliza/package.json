{
  "name": "@sigui/plugin-eliza",
  "version": "1.0.0",
  "main": "dist/index.js",
  "type": "module",
  "types": "dist/index.d.ts",
  "dependencies": {
    "@elizaos/core": "latest",
    "zod": "^3.23.8",
    "uuid": "^10.0.0"
  },
  "peerDependencies": {
    "@elizaos/core": "latest"
  },
  "devDependencies": {
    "tsup": "^8.0.2",
    "typescript": "^5.5.3",
    "@types/node": "^22.1.0",
    "@types/uuid": "^10.0.0"
  },
  "scripts": {
    "build": "tsup --format esm --dts",
    "dev": "tsup --format esm --dts --watch",
    "lint": "eslint --fix  --cache .",
    "clean": "rm -rf dist"
  },
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=22"
  }
}
